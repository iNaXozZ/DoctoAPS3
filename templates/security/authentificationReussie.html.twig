{% extends 'base.html.twig' %}

        {% block title %}Home{% endblock %}
        {% block stylesheets %}

        {% endblock %}

        {% block javascripts %}
            
        {% endblock %}

        {% block nav %}
        <nav class="navbar navbar-expand-sm navbar-light bg-primary">
            <a class="navbar-brand" href="{{path('connect')}}">DoctoAPS3</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">Coming Soon</a>
                </li>
                {% if is_granted('ROLE_PRO') %}
                 <li class="nav-item">
                    <a class="nav-link" href="/connect/affichersesconsulations/{{ app.user.id }}">Vos consultations</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_PATIENT') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('afficherlespro') }}">Liste des Professionnels de santé</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_PRO') %}
                 <li class="nav-item">
                    <a class="nav-link" href="/connect/planning/{{ app.user.id }}">Votre planning</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
                {% if is_granted('ROLE_PATIENT') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('consultation')}}">Prendre Rendez-Vous</a>
                </li>
                {% endif %}
                </ul>
            </div>
        </nav>
        {% endblock %}
        {% block body %}
        
        {% endblock %}
